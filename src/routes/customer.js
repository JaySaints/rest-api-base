let CustomerModel = require('../models/customer.model');
let express = require('express');
let router = express.Router();

// Create a new customer
// POST localhost:3000/customer
router.post('/customer', (rep, res) => {
    
});